
#include "INCLUDE"

char buf[1];

int main() {
  upb_arena *arena = upb_arena_new();
  size_t size;
  PROTO *proto = PROTO_parse(buf, 1, arena);
  PROTO_serialize(proto, arena, &size);
  return 0;
}
