
# Docker Toolchains

This directory contains definitions for C++ (not proto) toolchains that are used
for cross-compiling our release artifacts. Release artifacts include protoc and
Python binary wheels.

These toolchains have no relevance outside of our custom Docker images.
