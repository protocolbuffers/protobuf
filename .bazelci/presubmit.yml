---
tasks:
  ubuntu:
    platform: ubuntu2004
    shell_commands:
    - "sudo apt -y update && sudo apt -y install libreadline-dev cmake rsync"
    test_targets:
    - //...
  macos:
    platform: macos
    test_targets:
    - //...
